class Solution {
    public int largestElement(int[] nums) {
        int min=nums[0];
        int large=0;
        for(int i=1;i<nums.length;i++){
            if(nums[i]>min){
                large=nums[i];
            }
        }
            return large;
    }
}
